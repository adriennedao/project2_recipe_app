
<h2><%= link_to "Edit", ingredient_path(@ingredient) %></h2>

<%= link_to "Back", ingredient_path, class:'button' %>
<%= link_to "Edit", new_ingredient_path(@recipe), class:'button button-primary'%>
<%= link_to "Delete", @ingredient, method: :delete, data: {confirm: "Delete Ingredient?"}, class:'button'%>

<h2>Ingredient <%= link_to "+", ingredient_path(@recipe) %></h2>
